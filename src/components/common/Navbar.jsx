import React from "react";

const Navbar = () => {
  return (
    <header className="h-14 bg-white shadow px-4 flex items-center">
      <span className="font-semibold">Dashboard</span>
    </header>
  );
};

export default Navbar;
